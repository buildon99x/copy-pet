# Implementation Milestones

## M1 Tokens + primitives
Add theme tokens, rounded rect, focus border, text styles, source badge drawing.

## M2 Pet state machine
Implement mood transitions and XP event routing. No visual redesign yet.

## M3 Pet visuals
Draw idle/typing/sleeping/happy/levelup states using code-generated vectors matching SVG references.

## M4 Fish + nom
Implement source-badged fish queue, mouth-open timing, XP popup, sparkle/heart FX.

## M5 Panel shell
Implement dark premium panel shell, search row, header buttons, footer, resize grip.

## M6 Clip rows
Implement row states, selection, pin, quick-copy badges, source dots, delete hover halo.

## M7 Keyboard/IME/undo
Implement all keyboard semantics, IME-safe handling, undo stack, clear-confirm.

## M8 Settings/tray/i18n
Ensure all labels are in i18n and tray/context reflects real state.

## M9 Persistence/platform hardening
Clamp layout, save geometry, hotkey fallback UI, permissions, corrupt JSON recovery.

## M10 Preview + QA
Generate required frames and pass QA checklist.
